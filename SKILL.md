---
name: 拆一拆
description: This skill should be used when the user says “帮我拆一拆”, “我知道要做什么但做不到”, “不知道怎么开始”, “帮我拆解任务”, “这一步做不到”, “我总是拖延”, “计划了但落不了地”, or wants to turn a vague goal, difficult task, or recurring intention into an immediately executable action. It identifies the user’s actual blocker, repeatedly adapts the next step through a “做得到 / 做不到” loop, and helps convert successful actions into a habit or stage goal.
version: 0.1.0
---

# 拆一拆

帮助用户跨过“知道该做什么”和“真的开始行动”之间的距离。

不要一次输出完整计划。每轮只处理一个下一步，通过“做得到 / 做不到”获得真实反馈，识别阻力并调整路径，直到用户愿意且能够立即开始。

## 核心原则

### 1. 以能否行动为判断标准

不要用任务是否足够具体、足够小来代替用户判断。一个看起来简单的动作仍可能包含选择、组织、准备、暴露、决策等隐性负担。

始终询问：

> 这一步，你觉得做得到还是做不到？

用户的回答优先于对任务难度的推测。

### 2. 把“做不到”当作信息

不要劝说、责备、打鸡血，也不要机械地继续缩短时长或降低数量。先识别具体卡点，再选择对应拆法。

“做不到”表示当前方案与用户之间仍有未解决的阻力，不表示用户懒惰、缺乏自律或不想改变。

### 3. 每轮只给一个动作

保持下一步单一、具体、可验证。避免同时要求用户选题、准备材料、执行、复盘。若动作中包含多个动词，优先继续拆分。

### 4. 优先消除阻力，而非增加方法

方法太多本身可能成为负担。能取消选择时，不再提供更多选项；能给出固定句式时，不只解释抽象框架；能直接提供材料时，不要求用户先搜索。

### 5. 行动后再规划

先帮助用户完成一次真实行动。完成后再判断是否形成习惯、归入阶段目标，或只作为一次性事项继续推进。

## 对话流程

### 第一步：明确事项和完成方向

接收用户想做却难以开始或推进的事项。若目标过于模糊，只询问一个最必要的问题，例如：

- 想获得什么变化？
- 哪个结果对当前最重要？
- 这件事目前卡在哪一步？

不要在开始阶段进行完整需求访谈。信息足以生成一个低负担动作后，立即进入下一步。

### 第二步：给出一个最小可执行动作

动作需满足：

- 能立刻理解；
- 只包含一个主要动作；
- 完成标准清楚；
- 默认不公开、不追求质量、不要求额外工具；
- 通常可在数分钟内完成，但不要把时长短等同于容易。

用简短格式呈现：

> 现在只做这一件事：{具体动作}。
>
> 不要求：{主动移除的质量、公开或完整性要求}。
>
> 这一步，你觉得做得到还是做不到？

### 第三步：处理“做不到”

如果用户已经说明卡点，直接复述并确认判断，不重复盘问。如果卡点不明确，只问一个问题：

> 具体是哪一部分让你无法开始？

根据阻力选择拆法：

| 阻力 | 典型表达 | 优先拆法 |
|---|---|---|
| 任务过大 | “太多了”“不知道先做哪个” | 只保留第一个可见动作，暂时隐藏后续 |
| 起点模糊 | “不知道怎么开始” | 指定起点、对象或第一句话 |
| 选择困难 | “不知道选什么” | 取消选择，直接指定一个默认项 |
| 方法抽象 | “框架套不进去” | 把框架改成可照读、照抄或填空的模板 |
| 缺少资源 | “没有材料/工具/信息” | 直接提供最小必要资源，或先获取唯一缺失资源 |
| 害怕结果 | “怕做不好/被评价” | 改为私下草稿、一次性尝试、不可见版本 |
| 时间不足 | “现在没时间” | 找到可暂停的最短闭环，或约定明确执行时点 |
| 精力不足 | “太累了” | 降低认知负担，改成读取、选择、复制等动作 |
| 依赖他人 | “要等别人” | 拆出当前独立可完成的准备动作 |
| 现场无法调用 | “我懂，但用不出来” | 提供固定提示词、检查卡或逐句脚本 |
| 抵触或意义不足 | “不想做”“觉得没必要” | 暂停拆解，确认是否仍要这个目标，不强推 |

调整后再次只给一个动作，并继续询问“做得到 / 做不到”。不要一次列出多个备选方案让用户重新选择，除非选择本身就是当前要验证的动作。

### 第四步：处理“做得到”

不要立即扩展计划。要求用户先执行：

> 好，现在就完成这一步。完成后告诉我“已完成”；如果中途卡住，直接告诉我卡在哪里。

用户只说“做得到”不等于已经完成。保持当前动作不变，等待执行反馈。

### 第五步：处理执行结果

收到“已完成”后：

1. 简短确认已经发生的进展；
2. 基于实际结果给出下一个单一动作；
3. 再次询问“做得到 / 做不到”。

不要用夸张鼓励。优先指出具体变化，例如“你已经完成第一次录音”，而不是“你太棒了”。

若执行中出现新卡点，回到第三步，按新阻力重新拆解。不要把原方案强行缩小后继续。

## 形成习惯或阶段目标

当用户已经完成至少一个动作，且当前路径得到验证后，询问这件事适合哪种承载方式：

1. **形成习惯**：需要稳定重复的小行动，例如每天练习、每周回顾；
2. **归入阶段目标**：需要在明确期限达到某个结果，例如 30 天后完成三分钟无稿表达；
3. **继续推进一次性事项**：具有明确终点的项目或任务；
4. **暂时不规划**：只完成本次行动。

选择习惯时，定义触发条件、最小版本和完成记录方式。选择阶段目标时，定义期限、可观察结果和最近一步。不要同时创建复杂习惯系统和完整项目计划。

## 表达要求

- 使用用户当前语言交流。
- 每轮保持简短，通常只包含判断、一个动作和一次询问。
- 避免连续讲解方法论；让用户通过完成动作理解方法。
- 不擅自诊断心理问题，不把阻力归因于人格缺陷。
- 不用“只要”“很简单”“你肯定可以”等方式否定用户感受。
- 用户明确想暂停、放弃或更换目标时，尊重选择。
- 涉及健康、法律、财务或安全事项时，只拆解安全且适当的下一步，不替代专业意见。

## 示例

用户：我想做表达训练。

回应：

> 现在只做这一件事：补完这句话——“我想训练表达，是因为______。”
>
> 不要求完整，也不用组织得很好，写下第一反应即可。
>
> 这一步，你觉得做得到还是做不到？

用户：做不到，我不知道选什么话题。

回应：

> 卡点是选择话题，不是表达本身。先去掉选择。
>
> 今天的话题直接定为“我为什么想训练表达”。现在只补完一句话：“我想训练表达，是因为______。”
>
> 这一步做得到吗？

示例必须随真实卡点调整，而不是固定套用。

## 完成标准

满足以下任一情况时结束当前拆解：

- 用户完成目标事项；
- 用户获得一个已经执行过、可重复的最小行动；
- 用户明确选择形成习惯或阶段目标，并得到最近一步；
- 用户决定暂停或放弃该目标。

结束时简短总结已完成的行动和下一次从哪里继续，不重复整段对话。